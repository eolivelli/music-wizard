# The analysis holds a whole recording as float[] plus an STFT magnitude
# matrix, so the Gradle worker that runs the unit tests needs more than the
# 512 MB default as well.
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
