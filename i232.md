Bookkeeping left by #196, recorded rather than silently carried.

Several javadoc sweeps in `mw-dsp` and `mw-transcribe` justify a constant by comparing configurations against per-bar root accuracy on `samples/gmajorblues.mp3`, scored on the recording's measured 27.15 s loop — the metric `BluesLoopIT.mostBarsCarryTheRightChord` asserts. Their top cell is **86.6%**.

#196 changed the beat grid, and chroma is averaged per tracked beat, so that anchor is now **85.7%**, and the span count that goes with it is 666 rather than 740. `BluesLoopIT` is updated; the sweeps are not, because every other cell in them would have to be re-measured too and each cell is a full twelve-minute analysis with a constant patched.

Sites, at the line numbers on `main` after #196 lands:

| file | lines | what |
|---|---|---|
| `ChordEstimator` | 54, 61, 68, 107, 146, 187, 191, 263 | the 86.6% anchor and the sweeps around it |
| `ChordEstimator` | 107, 120, 146 | 740 spans, and "2.4 chords a bar" derived from it (666 and 2.1) |
| `NnlsChroma` | 305, 307, 350 | 86.6%, the 42.7% it is compared against, and 77.7% |
| `NoteDictionary` | 101 | a sweep row ending 86.6% / 86.3% |
| `LogFrequencySpectrum` | 89 | the same |
| `AudioTranscriber` | 284, 294 | 42.7% to 86.6%, and 77.7% against 86.6% |
| `tools/score-chart.py` | 189, 215 | 32.9% to 24.4%, and 24.4% to 36.1% — the harmonic-rhythm figure under a rounded header |

The `score-chart.py` pair is now labelled in place as pre-#196 rather than left reading as current, because relabelling was cheap there; re-taking them means reimplementing the rounded-header derivation the code deliberately no longer does.

**None of the conclusions is in doubt** — the shifts these tables measure are tens of points and the anchor moved by one — so this is a truth-in-prose defect, not a wrong constant. The cheap correct fix is to re-run each sweep once and restate the tables; the cheaper wrong one is to subtract 0.9 from every cell, which would be inventing measurements.

A note in `ChordEstimator`'s class javadoc points here in the meantime.
